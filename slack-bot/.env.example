# Wayfinder Slack Bot Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# SLACK APP CREDENTIALS
# Get these from https://api.slack.com/apps
# =============================================================================

# Bot User OAuth Token (starts with xoxb-)
# Found under: OAuth & Permissions > Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Signing Secret
# Found under: Basic Information > App Credentials > Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret

# App-Level Token for Socket Mode (starts with xapp-)
# Found under: Basic Information > App-Level Tokens
# Create one with connections:write scope
SLACK_APP_TOKEN=xapp-your-app-token

# =============================================================================
# WAYFINDER CONFIGURATION
# =============================================================================

# URL of the Wayfinder web app (for "View in Wayfinder" links)
WAYFINDER_URL=http://localhost:3001

# Port for the bot to listen on (optional, default 3002)
PORT=3002

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Create a Slack App at https://api.slack.com/apps
#
# 2. Enable Socket Mode:
#    - Settings > Socket Mode > Enable Socket Mode
#    - Create an App-Level Token with connections:write scope
#
# 3. Add Bot Token Scopes (OAuth & Permissions > Scopes):
#    - app_mentions:read (to receive @wayfinder mentions)
#    - chat:write (to send messages)
#    - commands (for /wayfinder slash command)
#
# 4. Create Slash Command (Slash Commands > Create New Command):
#    - Command: /wayfinder
#    - Description: Query the Wayfinder knowledge graph
#    - Usage hint: [search|api|team|who|pattern|help] <query>
#
# 5. Subscribe to Events (Event Subscriptions > Subscribe to Bot Events):
#    - app_mention
#
# 6. Install app to workspace (Install App > Install to Workspace)
#
# 7. Copy tokens to this .env file
#
# 8. Run the bot: npm run dev
